import 'package:flutter/material.dart';

class Tabscreen extends StatelessWidget {
  const Tabscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: Text("Tags Screen")),
      ),
    );
  }
}
